<p id="notice"><%= notice %></p>

<h1>用户资料页</h1>
<table>
  <thead>
    <tr>
      <th>用户名</th>
      <th>邮箱</th>
      <th>用户权限</th>
      <th>性别</th>
      <th colspan="5"></th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.user_name %></td>
        <td><%= user.email %></td>
        <td><%= "普通用户" if user.power == 1 %></td>
        <td><%= "管理员" if user.power == 2 %></td>
        <td><%= "男" if user.gender_id == 1 %></td>
        <td><%= "女" if user.gender_id == 2 %></td>
        <td><%= link_to '用户资料页', user %></td>
        <td><%= link_to '编辑', edit_user_path(user) %></td>
        <% if user.power == 1 %>
          <td><%= link_to '删除', user, method: :delete, data: { confirm: '确定删除?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
<%= paginate @users %>

<br>

<%= link_to '首页', '/' %>
<%= link_to '注册', new_user_path %>
