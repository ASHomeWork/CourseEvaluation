<%= form_with(model: major, local: true) do |form| %>
  <% if major.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(major.errors.count, "error") %> prohibited this major from being saved:</h2>

      <ul>
        <% major.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :专业名称 %>
    <%= form.text_field :专业名称 %>
  </div>

  <div class="field">
    <%= form.label :学院 %>
    <%= form.collection_select :学院, College.order(:name), :id, :name , { :include_blank => '选择学院' }%>
  </div>

  <div class="actions">
    <%= form.submit "提交", data:{ confirm: '确定添加？' }%>
  </div>
<% end %>
